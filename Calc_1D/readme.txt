1) �������������� ��������� ��������� (������ �����, ������ ���������), �������� ���������;
2) ���������� ����������;
3) ������, �� ������� ���������� ��������, ������ ��������� (��� ������� ���������);
4) ��������� �� ��� r:
  ��������� ����� ���������;
  ���������� �������� ���������;
  ����� ���������;
  ��������� ��� �� ������ �������;
  ������������ �������� �� ������ �������;
  ����������� �������� �� ������ �������;
5) ��������� �� ��� z (���������� ��� r);
6) ��������� �� ��������� ��� (���������� ��� r);
7) ���������� �����;
8) ������� ������� ����, �������� ����� (������������) �� ���� ���� (���� �������� ����� �����).
9) ���-�� �������� (������ 0).

1) местоположение точечного источника (радиус петли, высота положения), мощность источника;

2) количество приемников;

3) высота, на которой расположен приемник, радиус приемника (для каждого приемника);

4) разбиение по оси r:

 начальная точка разбиения;

 количество отрезков разбиения;

 точки разбиения;

 начальный шаг на каждом отрезке;

 коэффициенты разрядки на каждом отрезке;

 направления разрядки на каждом отрезки;

5) разбиение по оси z (аналогично оси r);

6) разбиение по временной оси (аналогично оси r);

7) количество слоев;

8) верхняя граница слоя, значение проводимости (сигма) на этом слое (слои задаются снизу вверх).

9) 0